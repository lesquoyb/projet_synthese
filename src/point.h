#ifndef POINT_H
#define POINT_H
#include <iostream>
#include "vecteur.h"
#include <cmath>
#include "angle.h"
/*

using namespace std;

class Vecteur{

private:

    double _x, _y;

public:

    Vecteur(const Vecteur&);
    Vecteur(double x, double y);


    double getDist(const Vecteur &p)const{
        return sqrt( pow(_x - p._x,2) + pow(_y - p._y,2) );
    }

    bool operator==(const Vecteur&p)const{
        return (_x == p._x) &&(_y == p._y);
    }

    friend ostream& operator<<(ostream& , const Vecteur &);

    Vecteur getCoordEntieres()const{return Vecteur(round(_x),round(_y));}

    double getX()const{return _x;}
    double getY()const{return _y;}

};
*/



#endif // POINT_H
